<?xml version="1.0" encoding="utf-8"?>
<Project DefaultTargets="Build" ToolsVersion="12.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003" TreatAsLocalProperty="TaskFolder;PackSourceFolder;PackOutputFile;TaskAssembly">
  <Import Project="..\..\packages\SciterCore.PackFolder.0.0.55-alpha\build\any\SciterCore.PackFolder.props" Condition="Exists('..\..\packages\SciterCore.PackFolder.0.0.55-alpha\build\any\SciterCore.PackFolder.props')" />
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
    <ProjectGuid>{D60ACAE0-C246-4936-B92B-B756841DB545}</ProjectGuid>
    <OutputType>Library</OutputType>
    <RootNamespace>SciterCore</RootNamespace>
    <AssemblyName>SciterCore.Gtk</AssemblyName>
    <TargetFrameworkVersion>v4.5</TargetFrameworkVersion>
    <TargetFrameworkProfile />
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\Debug</OutputPath>
    <DefineConstants>DEBUG;GTKMONO,GTK</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>3</WarningLevel>
    <ConsolePause>false</ConsolePause>
    <DocumentationFile>bin\Debug\SciterCore.Gtk.xml</DocumentationFile>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <Optimize>true</Optimize>
    <OutputPath>bin\Release</OutputPath>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>3</WarningLevel>
    <ConsolePause>false</ConsolePause>
    <DefineConstants>TRACE;GTKMONO,GTK</DefineConstants>
    <DocumentationFile>bin\Release\SciterCore.Gtk.XML</DocumentationFile>
  </PropertyGroup>
  <PropertyGroup>
    <SignAssembly>false</SignAssembly>
  </PropertyGroup>
  <PropertyGroup>
    <AssemblyOriginatorKeyFile>
    </AssemblyOriginatorKeyFile>
  </PropertyGroup>
  <ItemGroup>
    <Reference Include="System" />
    <Reference Include="System.Drawing" />
    <Reference Include="System.IO.Compression" />
    <Reference Include="System.Net" />
  </ItemGroup>
  <ItemGroup>
    <None Include="packages.config" />
    <None Include="SciterCore.Gtk.dll.config">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>
  <Import Project="$(SolutionDir)Shared\SciterCore.Shared\SciterCore.Shared.projitems" Label="Shared" Condition="Exists('$(SolutionDir)Shared\SciterCore.Shared\SciterCore.Shared.projitems')" />
  <Import Project="$(MSBuildBinPath)\Microsoft.CSharp.targets" />

  <!-- LibConsole packaging -->
  <PropertyGroup>
    <TaskFolder>$(MSBuildThisFileDirectory)..\..\scripts\</TaskFolder>
    <PackSourceFolder>$(MSBuildThisFileDirectory)..\..\Shared\SciterCore.Shared\LibConsole</PackSourceFolder>
    <PackOutputFile>$(ProjectDir).sciter\LibConsole.bin</PackOutputFile>
    <TaskAssembly Condition="$([MSBuild]::IsOsPlatform(Windows))">packfolder.exe</TaskAssembly>
    <TaskAssembly Condition="$([MSBuild]::IsOsPlatform(OSX))">packOSX.sh</TaskAssembly>
    <TaskAssembly Condition="$([MSBuild]::IsOsPlatform(Linux))">packOSX.sh</TaskAssembly>
  </PropertyGroup>

  <Target Name="SciterPreBuild" BeforeTargets="PreBuildEvent">
    <MakeDir Directories="$(ProjectDir).sciter" />
    <Exec Command="$(TaskFolder)$(TaskAssembly) &quot;$(PackSourceFolder)&quot; &quot;$(PackOutputFile)&quot; -binary" />
  </Target>

  <Target Name="SciterEmbedLocal" BeforeTargets="PrepareForBuild">
    <ItemGroup>
      <EmbeddedResource Include="$(ProjectDir).sciter\LibConsole.bin">
        <LogicalName>LibConsole</LogicalName>
      </EmbeddedResource>
    </ItemGroup>
  </Target>

  <Import Project="..\..\packages\SciterCore.Binaries.0.0.55-alpha\build\any\SciterCore.Binaries.targets" Condition="Exists('..\..\packages\SciterCore.Binaries.0.0.55-alpha\build\any\SciterCore.Binaries.targets')" />

  <Target Name="EnsureNuGetPackageBuildImports" BeforeTargets="PrepareForBuild">
    <PropertyGroup>
      <ErrorText>This project references NuGet package(s) that are missing on this computer. Enable NuGet Package Restore to download them.  For more information, see http://go.microsoft.com/fwlink/?LinkID=322105.The missing file is {0}.</ErrorText>
    </PropertyGroup>
    <Error Condition="!Exists('..\..\packages\SciterCore.Binaries.0.0.55-alpha\build\any\SciterCore.Binaries.targets')" Text="$([System.String]::Format('$(ErrorText)', '..\..\packages\SciterCore.Binaries.0.0.55-alpha\build\any\SciterCore.Binaries.targets'))" />
    <Error Condition="!Exists('..\..\packages\SciterCore.PackFolder.0.0.55-alpha\build\any\SciterCore.PackFolder.props')" Text="$([System.String]::Format('$(ErrorText)', '..\..\packages\SciterCore.PackFolder.0.0.55-alpha\build\any\SciterCore.PackFolder.props'))" />
  </Target>
  
</Project>